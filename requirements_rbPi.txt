# Requisitos optimizados para Raspberry Pi 3B+ o superior
# Versiones compiladas y ligeras para ARM (ARMv7l/ARMv8l)

# Core dependencies - Detección y procesamiento de video
# opencv-python: versión completa con soporte GUI por si es necesario en el futuro
opencv-python==4.13.0.92
numpy==2.4.3

# MediaPipe para IA (detección facial/pose)
# Nota: Compilar desde fuentes en RPi puede tardar 2-3 horas
# Alternativa: Usar pre-compiladas desde piwheels.org si están disponibles
mediapipe==0.10.5

# API y servidor web (muy ligeros)
fastapi==0.116.1
uvicorn==0.35.0

# Control de cámaras y comunicación
pytapo==3.4.11
python-kasa==0.10.2
telepot==12.7
requests==2.33.0

# Configuración
python-dotenv==1.2.2

# Notas de instalación para Raspberry Pi:
# 1. Asegurar que el sistema está actualizado:
#    sudo apt-get update && sudo apt-get upgrade
#
# 2. Instalar dependencias del sistema:
#    sudo apt-get install python3-pip python3-dev libatlas-base-dev libjasper-dev libharfbuzz0b libwebp6 libtiff5
#    sudo apt-get install libopenjp2-7 libjasper1 libharfbuzz0b libwebp6 libopenjp2-7 libtiff5 libjasper1 libharfbuzz0b libwebp6
#
# 3. Crear venv:
#    python3 -m venv .venv_pi
#    source .venv_pi/bin/activate
#
# 4. Actualizar pip y setuptools:
#    pip install --upgrade pip setuptools wheel
#
# 5. Instalar requisitos (esto puede tardar 30-60 minutos en RPi 3B+):
#    pip install -r requirements_rbPi.txt
#
# 6. Si mediapipe falla, instalar pre-compilado:
#    pip install --index-url https://www.piwheels.org/simple mediapipe
#
# 7. Alternativa si problemas de memoria: usar swap temporal
#    sudo fallocate -l 2G /swapfile
#    sudo chmod 600 /swapfile
#    sudo mkswap /swapfile
#    sudo swapon /swapfile
